Create a single-file HTML webpage called "Equipment Replacement Info" with the following exact specifications. The page must be fully functional, mobile-responsive, and use a light/clean theme. Use no external dependencies except pdf-lib loaded via CDN (https://unpkg.com/pdf-lib@1.17.1/dist/pdf-lib.min.js).

=== OVERALL BEHAVIOR ===
- All form fields must auto-save to sessionStorage on every input/change event so data survives page refreshes.
- On page load, all fields must be restored from sessionStorage.
- Blank fields must be OMITTED entirely from the generated summary (no empty lines, no orphaned section headers).
- The page must be fully usable on mobile phones (responsive layout, stacked buttons on small screens, appropriate input types).

=== PAGE STRUCTURE (top to bottom) ===

1. TITLE: "Equipment Replacement Info" centered at top.

2. PASTE FROM CLIPBOARD BUTTON:
   - A purple button labeled "📋 Paste from Clipboard" centered below the title.
   - Hint text below: "Paste work order info to auto-fill Customer, Contact, Model, Serial, and Equipment ID"
   - On click, read clipboard text using navigator.clipboard.readText()
   - Parse the clipboard text line by line looking for "Key: Value" patterns
   - Map these keys to form fields:
     * "Title (Customer):" or any key containing "title" or "customer" → Customer Name field
     * "Contact:" → Point of Contact field
     * "Item No. (Model):" or any key containing "item no" or "model" → Model field
     * "Serial Number:" or any key containing "serial" → Serial Number field
     * "Equipment ID:" or any key containing "equipment id" or "equipment" → New Equipment ID field
   - Auto-filled fields should briefly flash with a subtle gray highlight animation (CSS keyframes, 1.5s fade)
   - Show toast with count of fields filled
   - Handle errors: empty clipboard, no matches found, clipboard access denied

3. SECTION: "Call Notes" (h2 with bottom border)
   Fields (all text inputs unless noted):
   - Tech Name (id="techName")
   - City (id="city)
   - Customer Name (id="customerName")
   - Point of Contact (id="poc")
   - IT Person (id="itPerson")
   - Phone Number (id="itPhone", type="tel")
   - Email (id="itEmail", type="email")
   - Location of Machine (id="location")
   - Model (id="model")
   - New Equipment ID (id="newEquipmentId")
   - Serial Number (id="serialNumber")
   - Equipment ID Being Replaced (id="equipmentId")

4. SECTION: "IP Info" (h3)
   - IP Method: dropdown select with options: "", "Reused", "Mac address reservation", "DHCP", "IP scanner", "Other"
     * When "Other" is selected, show a conditional text input "Specify Other" (hidden by default using CSS class toggle)
     * This conditional visibility must persist through sessionStorage restore on page load
   - IP Address (id="ip")
   - Subnet (id="subnet")
   - Gateway (id="gateway")
   - DNS 1 (id="dns1")
   - DNS 2 (id="dns2")
   - DNS 3 (id="dns3")

5. SECTION: "Settings Transfer" (h3)
   - Textarea: "How were settings transferred?" (id="settingsTransfer", min-height 60px)

6. SECTION: "Scanning Info" (h3)
   - Scan to Folder Type: dropdown with options: "", "SMB", "WebDAV", "FTP"
   - Fax?: radio buttons Yes/No (name="fax")

7. SUB-SECTION: "SMTP" (h4)
   - Server (id="smtpServer")
   - Port (id="smtpPort")
   - SSL/TLS: dropdown with options: "", "None", "SSL", "TLS", "STARTTLS"
   - Email Address (id="smtpEmail")
   - NOTE: No password field.

8. SECTION: "Reporting" (h3)
   - Two clickable links displayed as a list directly under the heading:
     * "MPS Monitor Portal ↗" → https://portal.mpsmonitor.com/
     * "Jot Form ↗" → https://form.jotform.com/250095273606153
     * "Vcare" -> https://apps.kmbizhubvcare.com/vcaremobile/app/index.html#login?backUri=search
     * "Sharepoint" -> https://pacificoa.sharepoint.com/sites/POALearningandDevelopment/SitePages/Remote-Monitoring-Tools.aspx
     * Always open in new tabs
   - MPS?: radio buttons Yes/No (name="mps")
   - Was manufacturer reporting enabled?: radio buttons Yes/No (name="mfgReporting")

9. SUB-SECTION: "Page Counts" (h4, under Reporting)
   - Two number inputs side by side (flexbox row, stack on mobile):
     * Black & White (id="bwCount")
     * Color (id="colorCount")
   - Below them: a styled total display showing "Total: X" that auto-updates live as either field changes
   - Total is calculated as Black & White + Color, displayed with toLocaleString() formatting
   - The total display should have a light gray background, rounded corners, with the number in bold blue

10. SECTION: "Additional Info" (h3)
    - Small gray hint text: "Fax details, user box, etc."
    - Textarea (id="additionalInfo")

11. SECTION: "Summary Output" (h3)
    - Read-only textarea with monospace font, light gray background (id="outputBox")
    - Also saved/restored from sessionStorage

12. BUTTON GROUP (flexbox row, stacks vertically on mobile):
    - "Generate Summary" (green, class btn-generate)
    - "Copy to Clipboard" (blue, class btn-copy)
    - "📄 Download Filled PDF" (yellow/gold, class btn-download)
    - "Clear All" (red/orange, class btn-clear)

=== GENERATE SUMMARY LOGIC ===
Build a plain text summary with these sections. ONLY include a section header if at least one field in that section has data. ONLY include individual lines if that field has data.

Format:
=== CALL NOTES ===
Customer Name: {value}
Point of Contact: {value}
IT Person: {value}
Phone: {value}
Email: {value}
Location of Machine: {value}
Model: {value}
New Equipment ID: {value}
Serial Number: {value}
Equipment ID Being Replaced: {value}

--- IP Info ---
IP Method: {value or custom "Other" text}
IP Address: {value}
Subnet: {value}
Gateway: {value}
DNS 1: {value}
DNS 2: {value}
DNS 3: {value}

--- Settings Transfer ---
How settings were transferred: {value}

--- Scanning Info ---
Scan to Folder Type: {value}
Fax: {value}

-- SMTP --
Server: {value}   
Port: {value}
SSL/TLS: {value}
Email: {value}

--- Reporting ---
MPS: {Yes/No}
Manufacturer reporting enabled: {Yes/No}
-- Page Counts --
Black & White: {number with commas}
Color: {number with commas}
Total: {number with commas}

--- Additional Info ---
{free text}


IMPORTANT: The MPS Monitor Portal and Jot Form links must NEVER appear in the summary output. They are only clickable links on the form.

If no fields have any data at all, show toast "No data to generate" and do not produce output.
On success, show toast "Summary generated!"

=== COPY TO CLIPBOARD ===
- Use navigator.clipboard.writeText() with fallback to document.execCommand('copy')
- Show toast on success/failure
- If output box is empty, show "Nothing to copy — generate summary first."

=== DOWNLOAD FILLED PDF ===
- Fetch a PDF file named "net_form_with_text_feilds.pdf" from the same directory as the HTML file
- Use pdf-lib (PDFLib global) to load and fill the PDF
- Map these exact PDF field names to web form values:
  * 'customer' ← Customer Name
  * 'equipID' ← New Equipment ID
  * 'model' ← Model
  * 'black' ← Black & White count
  * 'color' ← Color count
  * 'total' ← calculated total (bw + color, as string)
  * 'ip' ← IP Address
  * 'subnet' ← Subnet
  * 'gateway' ← Gateway
  * 'dns1' ← DNS 1
  * 'dns2' ← DNS 2
  * 'city' <- City
  * 'techName' <- techName
- Use form.getTextField(name).setText(value) for each
- Wrap each field fill in try/catch, log warnings to console for mismatches
- Do NOT flatten the form (leave fields editable)
- Save and trigger download with auto-generated filename: "Network_Install_{CustomerName}_{EquipID}.pdf"
  * Sanitize filename by replacing non-alphanumeric chars (except underscore, hyphen, period) with underscores
- If no mapped fields have data, show toast and abort
- Show toast with count of filled fields on success
- Show error toast if PDF fetch fails

=== CLEAR ALL ===
- Confirm with browser confirm() dialog
- Clear all text/select/textarea fields and sessionStorage entries
- Uncheck all radio buttons and remove their sessionStorage entries
- Clear the output box and its sessionStorage
- Reset total display to "0"
- Re-run conditional field visibility logic (hide "Other" IP method field)
- Show toast "All fields cleared."

=== RADIO BUTTON STORAGE ===
- Store radio values in sessionStorage with key prefix "radio_" + name (e.g., "radio_fax", "radio_mps")
- On load, restore checked state by matching stored value

=== TOAST NOTIFICATIONS ===
- Fixed position at bottom center of screen
- Dark background (#2d3436), white text, rounded corners
- Slides up with opacity transition (0.3s ease)
- Auto-hides after 2.2 seconds
- Used for all user feedback messages

=== STYLING REQUIREMENTS ===
- Font: system font stack (-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, sans-serif)
- Background: #f8f9fa
- Container: white card, max-width 640px, centered, rounded corners (12px), subtle shadow
- Inputs: 1px solid #dee2e6 border, 8px border-radius, blue focus ring (#74b9ff with 3px box-shadow)
- Buttons: no border, 8px border-radius, 0.95rem font, scale(0.98) on :active
- All select elements must have -webkit-appearance: none
- Radio buttons must have -webkit-appearance: radio (override the select styling)
- Mobile breakpoint at 480px: reduce padding, stack buttons and page count fields vertically

Output the complete HTML file in a single code block with all CSS in a <style> tag and all JavaScript in a <script> tag. No external files except the pdf-lib CDN.
